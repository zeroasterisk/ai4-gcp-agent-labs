# Lab 01 — ADK foundation + the fully-local stack. Verified (2026-07-17):
#   google-adk 2.4.0, google-cloud-aiplatform 1.161.0, mcp 1.x.
# The local stack uses ADK in-process primitives: skills (load_skill_from_dir), a local stdio MCP
# (mcp_server/server.py via `mcp`), in-memory sessions/memory, and UnsafeLocalCodeExecutor.
google-adk>=2.4,<3
google-cloud-aiplatform[adk,agent_engines]>=1.112,<2  # <2: guard against aiplatform 2.x (2.0.0 exists on PyPI, currently yanked)
mcp>=1.24,<2  # <2: mcp 2.0.0 is API-incompatible (no mcp.shared.session / mcp.server.fastmcp)
# src/nimbus/agent/schemas.py imports pydantic directly (it also arrives
# transitively with google-adk; declared so this lab stands alone).
pydantic>=2,<3
# test tooling
pytest>=8
