# Lab 03 — MCP migrated to Cloud Run + Agent Registry (skills carried managed from Lab 02).
# Verified 2026-07-17: google-adk 2.4.0, aiplatform 1.161.0, mcp 1.x, starlette 1.3.1.
# AgentRegistry needs BOTH the agent-identity and a2a extras (F-013).
google-adk[agent-identity,a2a]>=2.4,<3
# >=1.154 provides the agentplatform SDK (client.skills) for the carried-forward Skill Registry.
google-cloud-aiplatform[adk,agent_engines]>=1.154,<2  # <2: guard against aiplatform 2.x (2.0.0 exists on PyPI, currently yanked)
# needed to import/test the MCP server locally (server hosting deps also in mcp_server/requirements.txt)
mcp>=1.24,<2  # <2: mcp 2.0.0 is API-incompatible (no mcp.shared.session / mcp.server.fastmcp)
starlette>=0.37
uvicorn>=0.30
# src/nimbus/agent/schemas.py imports pydantic directly (it also arrives
# transitively with google-adk; declared so this lab stands alone).
pydantic>=2,<3
pytest>=8
